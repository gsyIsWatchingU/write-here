{
  "name": "@gsy/horizon-docs-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "让 AI 以个人 Token 调用 Horizon Docs 的 MCP 服务",
  "bin": {
    "horizon-docs-mcp": "src/index.js"
  },
  "scripts": {
    "check": "node --check src/index.js && node --check src/horizon-client.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
