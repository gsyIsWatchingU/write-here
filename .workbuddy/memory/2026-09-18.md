# 2026-09-18

## 收工：续做并上线 MCP 1.1.0 + 工作台搜索/最近访问

- 续做上一轮留在工作区未提交的改动，完成两大功能：
  1. 工作台文档搜索 + 最近访问排序：`backend/server.js` 新增 `lastViewedAt` 列迁移与 `/docs/search` 接口、打开文档写 lastViewedAt、列表按 `COALESCE(lastViewedAt, updatedAt)` 排序；前端搜索框(250ms 防抖)+骨架屏+组件预加载。
  2. MCP 1.1.0：`search_markdown_documents` / `read_document_content` / `edit_markdown_document` / `move_markdown_document` 四个工具及后端接口（大纲提取、并发 `expectedUpdatedAt` 保护、搜索/读取长度限制）。
- 验证：后端 21/21、MCP 4/4、前端 78/78 测试通过；`node --check backend/server.js` 通过；前端 `vite build` 通过。
- 提交：`a498b67`(搜索/最近访问)、`d1d246e`(MCP 1.1.0)、`642b68c`(STATUS 文档)；`git push origin main` 快进 12 个提交到 `642b68c`。
- 线上：`run/deployed-commit=642b68c` 与本地 HEAD 一致；`deploy/verify-public.sh` 全绿（/health、/、/login 均 200，协同与通知两条 WebSocket 连接成功，SQLite 10 个业务表，Supervisor 两进程 RUNNING）。公网：https://dollar-wendy-buildings-year.trycloudflare.com
- 坑：本机 shell PATH 部分损坏（`grep/head/tail/ls/sleep/dirname` 缺失），用 git/node/ssh 全路径或 `node -e` 读文件绕开；`node --test backend/` 带目录参数会卡死，需指定具体 `*.test.js` 文件。
- 下一步（沿用 STATUS）：Named Tunnel、完整流程人工验收、生产级安全方案。
