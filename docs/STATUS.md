# 项目状态

最后更新：2026-09-24

## 当前阶段

GPU 服务器演示部署和极简像素主题改造已完成，功能继续完善中。

## 当前目标

将随机公网地址升级为固定域名，并继续完善生产安全能力。

## 已完成

- 编辑页在 1200px 及以下改用按需展开的文档目录与大纲面板，分享页在 1000px 及以下收起侧栏，避免半屏正文被挤成竖排。
- 编辑页侧栏与标题跳转按实际页头高度定位；标题栏支持换行、返回按钮保持单行，手机工具栏保持横向滚动。

- 已实现个人文档、公开社区、分享链接和协作申请审批流程。
- 已接入 TipTap、Yjs 协同编辑以及独立通知 WebSocket。
- 已整理 README，补充启动方式、技术栈、目录结构和关键实现说明。
- 已补充项目协作与交接约定。
- 已将 REST、协同 WebSocket、通知 WebSocket 改为生产环境同源访问。
- 已增加前端静态托管、`/health` 健康检查和 GPU 服务器部署脚本。
- 已部署到 GPU 服务器 `/workspace/projects/write-here`，使用持久化 Node.js 20 和独立 SQLite 数据库。
- 已复用 `research-workbench` 的 Supervisor + Cloudflare Quick Tunnel 方案提供公网 HTTPS 访问。
- 已参考 `cli-list` 完成灰白、黑绿描边、等宽字体和直角控件的极简像素主题。
- 已完成登录页、工作台、编辑器和社区页的桌面与移动端响应式适配。
- 已增加 GitHub Actions CI/CD：`main` 推送后自动构建检查并部署到 GPU 服务器，部署时保留服务器独立数据库。
- 已将 SQLite 调整为 GPU 服务器唯一业务数据库，本地数据库退出 Git 跟踪。
- 注册密码统一要求至少 6 位，前后端均执行校验。
- 编辑器已取消手动保存按钮，标题或正文变化后自动保存，并展示保存状态。
- 已完成评论线程、回复、评论点赞、@提及、解决状态及可跳转的实时消息通知。
- 网站品牌已更新为 `Horizon Docs`，并在页头、登录页和浏览器标签中使用望远镜图标。
- 工作台已明确标注“消息通知”和“协作请求”入口，并限制两个下拉面板互斥展示。
- 工作台、社区和管理页的桌面内容区上限已由 960px 放宽到 1440px，减少宽屏两侧空白。
- 编辑页和分享页的正文宽度上限已由 800px 放宽到 1000px，减少宽屏两侧空白，移动端仍按可用宽度自适应。
- CI/CD 已增加部署后公网验收与 `run/deployed-commit` 版本标记，协作约定要求每次 `main` 推送后等待并验证线上结果。
- CI/CD 部署任务已改用 GPU 服务器自托管 Runner，绕开公网 Runner 无法访问内网 SSH 的限制。
- 编辑器已支持导入 `.md` / `.markdown` 文件，导入后自动保存并同步给协作者。
- Markdown 导入已兼容行首被转义的标题、引用和列表标记，并保留代码围栏内的原始内容。
- 编辑器工具栏支持一键识别正文中的 `#`～`######` 标记并转换为对应标题级别（2026-09-24 起扩展为识别并渲染各类 Markdown 格式，见下）。
- 文档大纲会在协同内容加载和 Markdown 导入后立即按正文标题刷新，并兼容监听注册前已完成同步的情况。
- 编辑页和分享页的大纲点击会将标题固定定位到正文可视区顶部；移动端跳转后自动收起侧栏。
- 编辑页已新增飞书式左侧文档目录，支持搜索、新建、文档切换、协作文档分组和返回工作台。
- 编辑页和分享页的大纲、评论标签各占一半宽度，左右侧面板均支持收拢并自动释放正文空间。
- 文档目录条目已统一左对齐，个人文档支持拖放排序并持久化；右侧收拢按钮已并入面板标签栏，不再越出边框。
- 右侧面板展开时收起按钮使用普通纸白底，仅面板收拢后的重新展开按钮使用绿色强调。
- 编辑页和分享页的大纲已通过 H1～H6 标签、递进字号、字重、缩进和一级竖线强化标题层级。
- 编辑器和可编辑分享页已支持光标所在块的操作柄，可转换块类型，并复制、上下移动或删除块。
- 工作台文档卡片使用带中文标签的分享与删除图标按钮，降低操作含义歧义。
- 已新增独立“题库”区域、题目发布快照、版本号、受令牌保护的内容 API 与算法训练关联链接。
- 已将统一账号改为本站邮箱密码表单：注册需邮箱验证码，邮箱唯一，昵称可选并支持邮箱找回密码。
- 已新增隔离嵌入阅读页；Algorithm Lab 可复用 WriteHere 题面渲染，题面发布与判题配置保持解耦。
- 新增题库接口使用 30 天会话令牌，不再仅依赖客户端传入的 `userId`。
- 编辑器和分享页已支持飞书式划词评论：选区浮动入口、正文高亮、右侧评论线程、双向定位、锚点跟随编辑及原文失效提示。
- 评论消息已覆盖评论、回复、@提及、点赞聚合、解决/重新打开，并支持引用原文、未读去重和权限撤销后的内容脱敏。
- 评论侧栏已移除重复外框，输入区与空状态改用轻分隔和更均衡的间距层级。
- 编辑器和可编辑分享页的代码块支持 `Tab` 缩进选中行、`Shift+Tab` 反缩进，未选中文字时可在光标处插入两个空格。
- 已新增个人 MCP Token 管理、Markdown 文档读写 API 和 stdio MCP 服务；Token 仅存 SHA-256 哈希，AI 不接触账号密码。
- 前端时间展示已统一按 UTC 解析服务器的 SQLite 时间，再转换为浏览器本地时区。
- 块操作柄绑定完整顶层内容块；可直接拖放整块，展开菜单时会持续高亮完整目标块，代码块不会按行或局部文本拆分操作。
- 浏览器 favicon 已与其他自有站点统一圆角白底，并按 GPT 标签栏图标比例增大望远镜四周留白。
- 编辑器、只读文档和分享页的代码块已增加右上角一键复制图标，并提供复制成功、失败状态与兼容复制方案。
- 编辑器划词选中时会同时显示评论浮层与块操作柄，块操作柄绑定选区起点所在块，保持局部评论与整块操作的入口分层。
- 块操作柄改为按「正在操作的块」判定显示，而不再按「编辑器是否聚焦」判定：点击操作柄会先让编辑器失焦，旧逻辑因此会在同一帧内收起操作柄，导致代码块等块类型的点击转换与整块拖放完全无响应。现在点击操作柄、展开菜单或拖动过程中操作柄保持可用，编辑器失焦只影响默认收起时机，点击正文外空白或按 `Esc` 才收起。
- 编辑器和可编辑分享页支持在相邻内容块间没有空行时点击空白自动补一行；已有空行时不重复新增。
- MCP 已支持按关键词检索文档、提取 Markdown 大纲、分段读取长文档、局部精确编辑及调整个人文档顺序；局部编辑支持按 `updatedAt` 检查并发更新。
- 普通文档和题目编辑地址已改用 32 位随机哈希标识；旧数字地址自动替换为哈希地址，内部数字主键、评论、分享与 Yjs 协同房间保持兼容。
- 编辑页的标题栏与格式工具栏已合并到同一吸顶容器，正文向下滚动时顶部两行始终可见；吸顶容器高度随标题栏换行自适应，不再依赖写死的偏移值。
- 编辑器与可编辑分享页已支持 GPU 语音转写：点击工具栏麦克风按钮开始/停止录音，浏览器把麦克风采集为 16 kHz 单声道 WAV 上传 `/asr/transcribe`，后端转发同机 GPU 的 `Qwen3-ASR-1.7B` 后插入光标处。
- 编辑器已新增“AI 润色”按钮：输入润色要求后，后端在服务器本机调用 `claude` CLI（`claude -p --output-format text`，经 `ANTHROPIC_BASE_URL` / `ANTHROPIC_API_KEY` 接入 GPU 模型 API 三方 Key），对整篇文档润色后回填正文并自动保存；正文先在前端经 `turndown` 转 Markdown 提交，返回 Markdown 经 markdown-it（含任务列表插件）回填，表格、代码块语言与任务列表结构保留。
- 编辑器和可编辑分享页支持在文档末尾补行：当末尾块无法用回车直接产生普通段落时（代码块、列表、引用、表格等），点击该块下方的空白会在文档末尾插入一个空段落并把光标放进去；末尾是段落或标题时保持原有的回车行为不变，补行后末尾变成空段落，再次点击空白不会继续插入。
- 工作台已支持按标题与正文搜索文档：新增 `/docs/search` 接口、前端搜索框（250ms 防抖）与空结果提示；打开文档时记录 `lastViewedAt`，工作台与协作列表按最近访问优先排序，文档卡片时间显示最近访问时间；编辑器新增打开时的骨架屏占位并预加载组件以减少加载等待。
- MCP 服务升级至 1.1.0：新增 `search_markdown_documents`（标题/正文关键词检索并返回摘要与大纲）、`read_document_content`（按行分段读取、返回完整大纲与总行数）、`edit_markdown_document`（replace/append/prepend 局部修改，支持 `expectedUpdatedAt` 并发保护）、`move_markdown_document`（调整文档顺序）四个工具；后端新增对应接口，含大纲提取、搜索长度与读取行数限制及并发更新校验。
- 已新增公开只读 Markdown 接口（`GET /docs/:id/raw` 与 `GET /doc/:id/raw`）：无需登录，`public` 或已创建分享记录的普通文档可直接返回 Markdown 原文（旧 HTML 文档返回纯文本），供任何支持抓取链接的大模型直接读取；题库文档不开放。
- 已新增远程 HTTP MCP 端点（`/mcp/:secret`，Streamable HTTP，stateless 模式）：后端挂载 `@modelcontextprotocol/node` 适配器，ChatGPT 等只支持远程 MCP 的客户端可填 `https://<站点>/mcp/<secret>` 直接连接；`MCP_HTTP_SECRET` 作访问密钥，未配置时端点 503，工具调用仍以 `HORIZON_DOCS_TOKEN` 鉴权后端。
- 已优化文档加载的传输与查询开销：后端启用响应压缩、为带哈希的构建产物配置长缓存、列表类接口只回传正文预览片段、SQLite 改为 WAL 并补齐热查询索引（详见验证结果 2026-09-19）。
- 文档已支持插图：新增 `POST /images/upload` 上传接口（magic number 校验、10 MB / 8000 万像素上限、sha256 内容寻址去重、tmp+rename 原子写，文件落 `db/uploads/` 且不进 Git）；编辑器图片由行内节点改为块级节点 `DocImage`（默认居中，对齐走 `data-align`），多图同行合并为 `ImageGroup`；支持工具栏选择、粘贴和拖放三种插入方式，一次最多 6 张、并发 3；旧文档里的行内 `<p><img></p>` 在 Yjs 同步和 Markdown 导入时自动提升为块级图片，不会丢图；同行图片按宽高比分配宽度，因此自然等高并铺满整行，容器变窄时自动折行，窄屏单列。
- 图片与图片组节点已补充明确选中态：保留常驻黑框，选中后增加绿色像素光环；图片组同时显示浅绿底色与外框，取消选中后恢复原样。
- 网站品牌由 `Horizon Docs` 更名为 `Lumi Doc`：浏览器标题、页头 Logo、登录页与分享页品牌文字同步更新；静态资源 `horizon-logo.png`/`horizon-icon.png` 经 `git mv` 重命名为 `lumi-logo.png`/`lumi-icon.png`，favicon 缓存版本号升至 v9。MCP 配置示例中的 server 名 `horizon-docs` 与环境变量 `HORIZON_DOCS_URL`/`HORIZON_DOCS_TOKEN` 属技术标识符，与服务器 `.env` 和 mcp 代码绑定，保持不变。

- 编辑器与可编辑分享页的块边界补行已补全为双向：点击第一个块上方的顶部空白可在文档开头补一行，仅当第一个块缺少「回车在块前新建一行」的出口（代码块、图片、列表、引用等；段落和标题保持默认光标行为），与末尾补行共用同一判据；顶部补行回调需传 ProseMirror EditorView（`editor.view`），分享页因标题占据顶部且无留白不接入。

- 2026-09-24：工具栏「识别 MD 标题」升级为「识别 MD 格式」：一键扫描正文中未渲染的 Markdown 块级标记并渲染为对应格式，覆盖标题（`#`～`######`）、引用（`>` 连续行合并）、分割线（`---`/`***`/`___`）、无序/有序/任务列表（`-`/`*`/`+`、`1.`、`- [x]`）、GitHub 风格管道表格（表头 + 分隔行，分隔行可省略竖线）与代码围栏（``` 保留语言）；内容中的行内标记（`**加粗**`、`*斜体*`、`~~删除线~~`、`[链接](url)`、`` `代码` ``）一并渲染。实现位于 `frontend/src/utils/markdownFormats.js`：仅处理纯文本段落，已有格式的块不动，空段落打断连续组，行首 `\` 转义标记兼容（与 Markdown 导入语义一致）；行内渲染直接消费 markdown-it 的 inline token 流构造 ProseMirror 节点（不依赖浏览器 DOM），并兼容 TipTap 的 `bold`/`italic` mark 命名。

## 下一步

1. 将 Cloudflare Quick Tunnel 迁移为自有域名的 Named Tunnel。
2. 手工验证注册登录、文档编辑、分享和协作审批完整流程。
3. 为密码存储、鉴权、限流和输入安全补充生产级方案。
4. 拆分较大的前端编辑器分块，并逐步补充自动化测试。
5. 部署后配置 Algorithm Lab 的 WriteHere 内网与公网地址，并完成跨站关联验收。
6. 把块操作柄的浏览器验收脚本（临时无头 Chrome + CDP 驱动）整理进仓库，补齐编辑器交互回归。

## 已知问题

- 新账号密码由统一账号中心哈希保存；旧版本地用户表仍保留兼容字段，后续可清理历史演示账号。
- 旧版 `db/docs.db` 仍存在于 Git 历史；如曾包含敏感数据，需要单独执行历史清理。
- 前端构建提示编辑器分块超过 500 kB，需要后续按需拆分。已通过压缩降到约 278 KB 传输量，但包本身仍未做 `manualChunks` 拆分（`lowlight` 全量语言是主要体积来源）。
- 文档正文需等 Yjs 协同 sync 完成后才渲染，未接入本地持久化；网络较差时正文区会停在骨架屏。
- 打开非本人文档时存在串行往返：先取文档，再取协作状态，最后等协同同步。
- 2026-09-19 一轮优化未取得 GPT 第二意见：专用 Chrome 的 ChatGPT 通道报未登录且页面加载失败，方案改由仓库实测证据确定。
- `/uploads` 是不带权限校验的静态目录（拿到 URL 即可读），后续需补带鉴权的图片读取层；已上传但文档里已删除的图片不做孤儿回收。
- Quick Tunnel 使用随机地址，隧道或服务器重启后地址可能变化。
- GPU 服务器容器重启后需确认 Supervisor 与两个 WriteHere 进程已恢复。
- GPU 自托管 Runner 必须保持在线；当前由 Supervisor 的 `github-actions-write-here` 进程守护。
- 退出登录只保证本地登出即时生效；若 `/logout` 请求失败（离线、后端不可达），服务端会话记录会残留到过期，期间该 token 仍有效。当前演示级认证下可接受，做生产级鉴权时应一并收紧（例如短会话有效期 + 服务端主动失效）。

## 验证结果

- 2026-09-03：前端 `vite build` 通过，共转换 421 个模块。
- 2026-09-03：`node --check backend/server.js` 通过。
- 2026-09-08：本地前端生产构建与后端语法检查通过。
- 2026-09-08：服务器 `/health`、首页、登录页均返回 200。
- 2026-09-08：协同与通知 WebSocket 均连接成功，SQLite 初始化为 7 个业务表。
- 2026-09-08：Supervisor 托管与公网 HTTPS、WSS 验证通过。
- 2026-09-08：极简像素主题前端构建通过，并完成 1280px 桌面与 390px 移动端视觉验收。
- 2026-09-08：CI/CD 工作流与安全发布脚本已完成本地检查；首次运行前需配置 GitHub Actions Secrets。
- 2026-09-08：GPU 数据库账号清理前已创建备份；清理后可重新注册至少 6 位密码的新账号。
- 2026-09-09：实时自动保存改造通过前端构建、后端语法检查和本地编辑器页面检查。
- 2026-09-09：评论/通知系统通过前端构建、后端语法检查及临时 SQLite 接口闭环验证。
- 2026-09-09：`Horizon Docs` 品牌改造通过前端构建，并完成桌面端与 390px 移动端视觉检查。
- 2026-09-09：工作台通知入口可读性与面板防重叠改造通过前端构建。
- 2026-09-09：列表页内容区放宽通过前端构建，并完成 1864px 桌面与 390px 移动端视觉检查；移动端无横向溢出。
- 2026-09-09：`fae7ba0` 的 CI 构建成功，但部署配置检查失败；GPU 公网 HTTP、WSS、SQLite 与 Supervisor 现状正常，页面代码仍为旧的 960px 版本。
- 2026-09-09：Markdown 导入功能通过前端生产构建。
- 2026-09-09：GitHub 公共 Runner 访问内网 GPU SSH 超时；已改用标签为 `write-here-gpu` 的自托管 Runner，并清理不再使用的 5 个 SSH Secrets 与专用公钥。
- 2026-09-09：`33c35b1` 的 `main` 推送已完成构建、产物上传、自托管部署和公网验收；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，HTTP、两条 WebSocket、8 个 SQLite 业务表及应用、隧道、Runner 的 Supervisor 状态均通过。
- 2026-09-10：文档加载与 Markdown 导入后的大纲刷新修复通过前端生产构建和后端语法检查；线上公开文档已确认保存为真实 `h2`，并补充已完成同步时的立即刷新处理。
- 2026-09-10：Markdown 转义标记兼容修复通过 2 项回归测试、前端生产构建和后端语法检查。
- 2026-09-10：`4385860` 的 Markdown 导入修复已通过 CI/CD #13 的构建与 GPU 部署；后续最新 `main`（`529fa4b`）与服务器 `run/deployed-commit` 一致，公网 HTTP、两条 WebSocket、8 个 SQLite 业务表及应用、隧道、Runner 的 Supervisor 状态均通过。
- 2026-09-10：文档卡片分享与删除按钮可识别性改造通过前端生产构建。
- 2026-09-10：块级转换菜单通过正文转标题 1、自动保存与大纲同步验收；390px 移动端无横向溢出。
- 2026-09-10：块操作补全通过复制、上下移动、首尾禁用和自动保存浏览器验收；`e3c5c64` 已部署，公网 HTTP、两条 WebSocket、9 个 SQLite 业务表及 Supervisor 状态均通过。
- 2026-09-10：题库融合通过前端生产构建、后端语法检查及临时 SQLite 发布快照测试；尚未部署和浏览器端到端验收。
- 2026-09-10：划词评论通过 4 项锚点单元测试、前端生产构建、后端语法检查及临时 SQLite 接口闭环验证；桌面端与 390px 移动端完成浏览器验收，通知可跳转并定位原文。
- 2026-09-11：Markdown 标题一键识别通过 2 项新增单元测试、前端生产构建和后端语法检查；桌面端与 390px 移动端点击验收通过，大纲即时刷新且代码块不受影响。
- 2026-09-11：评论侧栏视觉优化通过前端生产构建，并完成桌面端、输入聚焦状态与 390px 移动端视觉检查；移动端无横向溢出。
- 2026-09-11：代码块 Tab 缩进通过 5 项新增单元测试、13 项前端全量测试、前端生产构建、后端语法检查和接口测试。
- 2026-09-11：时间时区修复通过 3 项新增单元测试、前端生产构建、后端语法检查和桌面端页面视觉检查；SQLite UTC 时间在东八区正确增加 8 小时显示。
- 2026-09-11：站内统一账号表单通过前端 16 项测试、后端 1 项接口测试、生产构建和后端语法检查；桌面端与 390px 移动端登录、注册布局验收通过。
- 2026-09-11：`d7a2cdc` 的代码块 Tab 缩进已通过 CI/CD #28 构建与 GPU 部署；服务器提交标记一致，公网 HTTP、两条 WebSocket、9 个 SQLite 业务表及应用、隧道、Runner 的 Supervisor 状态均通过。
- 2026-09-12：MCP 接入通过后端 2 项接口测试、MCP 3 项客户端/stdio 握手测试、前端 16 项测试、生产构建和后端语法检查；桌面端与 390px 移动端 Token 管理弹窗验收通过。
- 2026-09-12：`e3abbc2` 的 MCP 接入已通过 CI/CD #33 构建与 GPU 部署；服务器提交标记一致，公网 MCP API 返回预期鉴权结果，公网前端资源包含 MCP 入口，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：大纲固定位置跳转修复通过 18 项前端测试、生产构建和后端语法检查；桌面端标题定位到 120px，390px 移动端定位到 170px 并自动收起侧栏，浏览器控制台无错误。
- 2026-09-14：`c34d0e5` 的大纲跳转修复已通过 CI/CD #35 构建与 GPU 部署；服务器提交标记一致，公网静态资源包含新定位逻辑与桌面/移动端偏移，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：块操作柄对齐与目标行高亮通过 18 项前端测试、生产构建和后端语法检查；桌面端与 390px 移动端中心误差约 0.3px，普通段落及列表项均只高亮当前行，控制台无错误。
- 2026-09-14：`a2453f9` 的块操作柄修复已通过 CI/CD #37 构建与 GPU 部署；服务器提交标记一致，公网静态资源包含 30px 操作柄与装饰层高亮逻辑，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：浏览器 favicon 二次统一为 85% 圆角白底与 56% 居中主体，通过 39 项前端测试、生产构建、后端语法检查及 16px/32px 视觉验收；缓存版本已更新。
- 2026-09-14：`06a402e` 的 favicon 改造已通过 CI/CD #39 构建与 GPU 部署；服务器提交标记一致，公网首页与 favicon 均返回 200，线上 SVG 包含圆角白底和居中缩放参数，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：代码块一键复制通过 21 项前端测试、生产构建和后端语法检查；桌面端与 390px 移动端图标布局验收通过，点击后正确显示复制成功状态且移动端无横向溢出。
- 2026-09-14：`b432d00` 的代码块一键复制已通过 CI/CD #41 构建与 GPU 部署；服务器提交标记一致，公网静态资源、HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：五站 favicon 完成统一对照；Horizon Docs 望远镜较前版稍大，21 项前端测试、生产构建、后端语法检查及 16px/32px 视觉验收通过。
- 2026-09-14：`cb85a29` 的 favicon 尺寸统一已通过 CI/CD #43 构建与 GPU 部署；服务器提交标记一致，公网 favicon 包含统一圆角白底与放大参数，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：划词与块操作双入口通过 24 项前端测试、生产构建和后端语法检查；桌面端与 390×844 移动端均可同时显示评论浮层和块操作柄，块菜单展开后选区仍保留，移动端无横向溢出且控制台无警告或错误。
- 2026-09-14：`bf61516` 的划词与块操作双入口已通过 CI/CD #45 构建与 GPU 部署；服务器提交标记一致，公网 HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：编辑页和分享页正文宽度放宽通过 24 项前端测试、生产构建和后端语法检查；1809px 下正文宽度为 1000px 且不与右侧大纲重叠，390px 下正文仍按可用宽度自适应。
- 2026-09-14：文档目录与侧栏收拢改造通过 27 项前端测试、生产构建和后端语法检查；1280px 下大纲、评论各为 170.5px，右栏收拢后正文由 561px 扩至 937px，390×844 移动端抽屉、底部面板及横向溢出检查通过。
- 2026-09-14：`1363e1d` 的文档目录与侧栏收拢改造已通过 CI/CD #48 构建与 GPU 部署；服务器提交标记一致，公网静态资源包含文档目录、面板收拢与标签等分规则，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：文档目录对齐、拖放排序与右栏按钮归位通过 30 项前端测试、3 项后端测试、前端生产构建和后端语法检查；自动浏览器验收因本机浏览器控制服务连接失败未执行。
- 2026-09-14：块间空白点击插入通过 4 项新增单元测试、34 项前端全量测试、3 项后端测试、3 项 MCP 测试、前端生产构建和后端语法检查；自动浏览器验收因本机浏览器控制服务连接失败未执行。
- 2026-09-14：代码块整块操作与拖放通过 3 项新增回归测试、37 项前端测试、3 项后端测试、3 项 MCP 测试、生产构建和后端语法检查；拖放事务会原样移动完整代码块节点并显示落点，自动浏览器验收因本机浏览器控制服务连接失败未执行。
- 2026-09-14：`2788885` 的代码块整块操作与拖放已通过 CI/CD #52 构建与 GPU 部署；服务器提交标记一致，公网静态资源包含整块拖放入口，HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过。
- 2026-09-14：右侧收起按钮状态色调整通过 37 项前端测试、生产构建和后端语法检查；自动浏览器验收因本机浏览器控制服务连接失败未执行。
- 2026-09-14：块间空行去重通过 2 项新增回归测试、39 项前端全量测试、生产构建和后端语法检查；已有空文本块时再次点击相邻空白不会继续插入。
- 2026-09-14：大纲标题层级强化通过 39 项前端测试、生产构建和后端语法检查；桌面端与 390×844 移动端展开态视觉验收通过，H2/H3 的标签、字号、字重和缩进差异清晰且无横向溢出。
- 2026-09-14：MCP 文档检索、分段读取、局部编辑和排序通过后端 MCP 接口测试、MCP 客户端测试、stdio 握手与工具调用测试及语法检查。
- 2026-09-14：文档哈希标识改造通过 40 项前端测试、5 项后端测试、4 项 MCP 测试、前端生产构建和后端语法检查。
- 2026-09-14：`5006b57` 的文档哈希标识改造已通过 CI/CD 构建与 GPU 部署；服务器提交标记一致，公网 HTTP、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 状态均通过，并已由 stdio MCP 创建哈希文档、验证哈希与旧数字 ID 读取兼容。
- 2026-09-15：编辑页吸顶改造通过 45 项前端测试、前端生产构建和后端语法检查；构建产物确认 `.editor-page` 使用 `overflow-x: clip`、`.editor-sticky-head` 使用 `position: sticky`，标题栏已移除原有的无效 sticky 声明。自动浏览器验收因本机未安装浏览器未执行。
- 2026-09-15：块操作柄失焦可用性修复通过 5 项新增单元测试、45 项前端全量测试、前端生产构建和后端语法检查；使用临时无头 Chrome（CDP 驱动）挂载块菜单与代码块的验收页完成 11 项浏览器验收并全部通过：代码块入块出现操作柄、点击操作柄打开转换菜单并高亮目标块、`Esc` 收起菜单且保留操作柄、代码块转正文、点击空白处收起操作柄、代码块向上/向下拖动换位、正文点击打开菜单、菜单项「下移」生效、正文整块拖动换位。修复前同一脚本可复现点击无响应与拖动失败。
- 2026-09-15：`a493e5e` 的块操作柄失焦可用性修复已通过 CI/CD 构建与 GPU 部署；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，公网 HTTP（`/health`、`/`、`/login`）、两条 WebSocket、10 个 SQLite 业务表及应用、隧道、Runner 的 Supervisor 状态均通过，线上 `outlineNavigation` 产物包含新的 `Pointerenter` 交互与 `Escape` 收起逻辑。
- 2026-09-16：语音转写通过 10 项新增前端测试（前端共 54 项）、6 项新增后端测试（后端共 12 项）、前端生产构建和后端语法检查；经 SSH 隧道连接 GPU 的 8001 端口实测：未登录 401、非法音频 400、真实 ASR 返回 200（2.5 秒音频耗时 117 ms）、上游不可用时 502。
- 2026-09-16：`28fa331` 的语音转写已通过 CI/CD 构建与 GPU 部署；服务器 `run/deployed-commit` 与本地一致，公网 HTTP（`/health`、`/`、`/login`）、两条 WebSocket、10 个 SQLite 业务表及应用、隧道的 Supervisor 状态均通过，线上产物包含 `Ctrl+Alt+M` 与 `asr/transcribe`，线上接口未登录与无效令牌均返回 401。
- 2026-09-16：语音转写快捷键由三键组合改为**双击 `Ctrl`** 开始/停止（另留按住 `F2` 与工具栏按钮）：通过 11 项录音与快捷键工具测试（前端共 55 项）、前端生产构建和后端语法检查；双击判定会排除「按住 `Ctrl`」「`Ctrl+C` 等组合」和重复 keydown，避免误触发。
- 2026-09-17：AI 润色通过后端 9 项新增测试（后端共 21 项）、前端 9 项新增测试（前端共 71 项）、前端生产构建和后端语法检查；本地冒烟测试验证 `/ai/polish/config` 与 `/ai/polish` 未登录 401、未配置时 `configured=false` 且提交返回 503。尚未在 GPU 服务器安装 claude CLI 并配置三方 Key，真实调用链路待部署配置后验收。
- 尚未执行注册登录、文档编辑、分享和协作审批的完整浏览器验收。

- 2026-09-17：编辑器与可编辑分享页支持文档开头空行退格删除：光标位于文档第一行空文本块（空段落或空列表项）时按 Backspace 直接删除该行；通过 9 项新增单元测试（前端共 71 项）、前端生产构建，后端无改动。
- 2026-09-17：文档末尾点击空白补行通过 7 项新增单元测试（前端共 78 项）、前端生产构建和后端语法检查；用临时无头 Chrome（CDP 驱动）加载真实 TipTap 编辑器完成 12 项浏览器验收并全部通过：空代码块为唯一块时点击下方空白新增普通段落且光标落在新行（选区位置与期望一致）、点击编辑器底部更下方的空白同样生效、末尾是代码块或列表时补行、末尾是段落或标题时不补行、点击代码块内部不补行、块间空白点击回归通过、补行后再次点击末尾空白不再插入；同时实测确认光标在代码块内按回车只得到块内换行、标题与段落末尾按回车才能直接产生普通段落。
- 2026-09-17：`8135a71` 的文档末尾补行已通过 CI/CD 构建与 GPU 部署；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，协同与通知两条 WebSocket 连接成功，SQLite 10 个业务表，`write-here` 与 `cloudflared-write-here` 均为 RUNNING），部署包内 `frontend/src/utils/blockGapInsertion.js` 含 `findTrailingInsertionIndex`、`shouldInsertTrailingParagraph`、`TRAILING_ESCAPE_TYPES` 三个新符号及其测试。
- 2026-09-18：工作台搜索与最近访问排序、MCP 1.1.0 工具（搜索/分段读取/局部编辑/调整顺序）通过前端 78 项测试、后端 21 项测试、MCP 4 项测试、前端生产构建和后端语法检查；`d1d246e` 已通过 CI/CD 构建与 GPU 部署，服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，协同与通知两条 WebSocket 连接成功，SQLite 10 个业务表，`write-here` 与 `cloudflared-write-here` 均为 RUNNING）。
- 2026-09-18：公开只读 Markdown 接口（`/docs/:id/raw` 与 `/doc/:id/raw`）通过 5 项集成测试（public+Markdown 返回 `text/markdown`、private 无分享 404、private 已分享 200、旧 HTML 文档返回 `text/plain`、不存在哈希 404）及 `node --check` 语法检查；已随 `4967b5c` 部署至 GPU 服务器。
- 2026-09-18：远程 HTTP MCP 端点通过端到端测试（`@modelcontextprotocol/client` 连接 `http://127.0.0.1:3213/mcp/<secret>`：错误密钥 403、initialize 成功、8 个工具列出、`create_markdown_document` 创建与 `get_markdown_document` 回读均通过，已纳入 `mcp` 包 `npm test`）；`node --check` 通过；已随 `4967b5c` 部署至 GPU 服务器并通过公网验收。
- 2026-09-18：`4967b5c` 已通过 CI/CD 构建与 GPU 部署：服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`write-here` 与 `cloudflared-write-here` 均 RUNNING；修复线上 HTTP MCP 端点 500（`deploy/start.sh` 补充 `npm ci --omit=dev --prefix mcp`）；服务器 `.env` 新增 `MCP_HTTP_SECRET`/`HORIZON_DOCS_URL`/`HORIZON_DOCS_TOKEN`（备份 `.env.bak-mcp-*`），`api_tokens` 新增 `MCP-HTTP-gsy`（userId=2）；公网实测 `/mcp/<secret>`——错误密钥 403、`@modelcontextprotocol/client` 连接成功并列出全部 8 个工具。
- 2026-09-19：文档加载性能优化通过后端 25 项测试（含新增 4 项静态交付与传输量回归测试）、`node --check` 语法检查；实测最大 chunk `outlineNavigation` 由 835.8 KB 压到 278.3 KB，`assets/` 返回 `public, max-age=31536000, immutable`、`index.html` 返回 `no-cache`，列表接口正文由 10407 字符降为 200 字符预览，`journal_mode=wal`，`idx_docs_visibility`/`idx_shares_docId`/`idx_comments_docId`/`idx_notifications_userId` 四个索引已建立（未重复建 `docs(userId, kind)`，已有 `idx_docs_kind_owner` 可复用）。
- 2026-09-20：文档插图功能通过前端 93 项测试（新增 14 项：布局比例、插入结构、旧 HTML 迁移、上传保序/失败隔离）、后端 32 项测试（新增 7 项：类型识别、分片路径、原子写、401/400/413、去重与元数据落库）、MCP `npm run check` 与 5 项测试、前端生产构建、`node --check backend/server.js` 与 `backend/imageUpload.js`。
- 2026-09-20：用临时无头 Chrome（CDP 驱动真实 `input[type=file]` + 真实后端 + 真实 PNG）完成 14 项浏览器验收并全部通过：单张插入 `<figure class="doc-image" data-align="center">`、图片 URL 为 `/uploads/xx/yy/<64hex>.png`、图片真实加载（642×163）、左右留白各 369px（视觉居中）；两张合成 `doc-image-group` 且同行等高（893×230 与 483×230，行宽 1384 = 容器宽，未重叠）、`flex-grow` 按宽高比取 3.94 / 2.13 而非等分；`setContent(getHTML())` 往返结构不变（刷新不丢图）；旧行内 `<p><img src="/uploads/legacy.png"></p>` 被提升为块级 figure 而非丢弃。验收页与脚本均为临时文件，已删除。
- 2026-09-20：`39aa79c` 的文档插图功能已通过 CI/CD 构建与 GPU 部署；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，协同与通知两条 WebSocket 连接成功，SQLite 11 个业务表——新增 `images`，`write-here` 与 `cloudflared-write-here` 均 RUNNING）；线上实测 `POST /images/upload` 未登录返回 401，`db/uploads/` 目录已创建且可写，线上 `frontend/dist` 产物包含 `doc-image`（JS）与 `doc-image-group`（CSS）。
- 2026-09-19：`cd15eec` 已通过 CI/CD 构建与 GPU 部署；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，协同与通知两条 WebSocket 连接成功，SQLite 10 个业务表，`write-here` 与 `cloudflared-write-here` 均 RUNNING）；公网实测最大 chunk `outlineNavigation-DJYAhHe4.js` 由 855,894 B 压到 284,986 B（-66.7%），响应头为 `content-encoding: gzip` + `cache-control: public, max-age=31536000, immutable`，`index.html` 为 `no-cache`。
- 2026-09-21：图片与图片组选中态通过 96 项前端测试和生产构建；临时无头 Chrome 挂载真实 TipTap 编辑器完成 5 项验收：未选中无光环、点击图片产生节点选区并显示绿黑双层光环、图片组显示绿色外框与浅绿底色、取消选中后光环消失，验收临时文件已清理。
- 2026-09-21：修复「点击退出没反应」通过前端 109 项测试（新增 5 项退出回归测试）和生产构建；用临时无头 Chrome + CDP 搭受控复现环境（真实 router 守卫 + 真实 Home/Login 组件，仅把后端替换为可控假服务并保留真实会话 cookie 语义）跑通五场景 19 项：正常退出、`/logout` 抛错、服务端会话未失效、退出后重新登录仍可进首页、`/logout` 挂住不返回，各场景均停在 `/login` 且本地凭据清空、无未捕获异常。修复前实测前四场景失败（`path` 停在 `/`）。根因有两条：`clearUser` 用 `try/finally`（`finally` 不吞异常，`/logout` 失败会把异常抛给调用方，而各页 `await clearUser()` 之后才跳转，后端不可达或卡住就永远走不到跳转）；以及 `Login` 的 `onMounted` 无条件调 `/me` 并用残留会话 `replace('/')` 把用户弹回首页。改动：`clearUser` 先同步清 `localStorage` 再降级处理请求失败，新增 `sessionStorage` 退出标记，`Login` 主动退出时不再自动跳回。
- 2026-09-21：`e580d1f` 的退出修复已通过 CI/CD 构建与 GPU 部署（`构建检查` 与 `部署到 GPU 服务器` 两个 job 均 success）；服务器 `run/deployed-commit` 与本地 `HEAD` 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，协同与通知两条 WebSocket 连接成功，SQLite 11 个业务表，`write-here` 与 `cloudflared-write-here` 均 RUNNING）；线上 `api-jvGBI1p9.js` 实测包含修复后的 `clearUser`（先清 localStorage、`catch` 降级为 `console.warn`）、`loggedOut` 标记函数与 `setUser` 清理逻辑，`Login` chunk 确认从该模块导入。

- 2026-09-23：响应式修复通过 109 项前端测试、生产构建及后端语法检查；浏览器加载真实编辑页/分享页组件（使用本地模拟数据，隔离协同连接），完成 320–1920px 的 26 组尺寸、151 项布局检查，正文宽度、横向溢出、侧栏开关与吸顶检查全部通过。
- 2026-09-23：品牌由 `Horizon Docs` 更名为 `Lumi Doc`（浏览器标题、页头/登录/分享页品牌文字，图片 git mv 为 lumi 前缀，favicon 升至 v9）；同时因 GitHub Actions 将默认 Node 升至 24 导致 `mcp.test.js` 时序敏感断言失败，在 ci job 加 `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"` 固定 Node 20。`d7d96f2` 已通过 CI/CD 两个 job（构建检查 + 部署到 GPU 服务器），服务器 `run/deployed-commit` 与本地 HEAD 一致，`deploy/verify-public.sh` 全绿（公网 `/health`、`/`、`/login` 均 200，两条 WebSocket 连接成功，SQLite 11 个业务表，`write-here` 与 `cloudflared-write-here` 均 RUNNING）；公网登录页 `<title>` 为 `Lumi Doc - 轻量在线文档，即写即存`，页面无 Horizon 残留，favicon 指向 `lumi-icon.png`。
- 2026-09-24：编辑器顶部补行（点击第一个块上方空白在文档开头补行）通过 8 项新增单元测试（前端共 117 项）与生产构建；用临时无头 Chrome + CDP 加载真实 TipTap 编辑器（复用 `insertParagraphInClickedGap` 接线）完成 8 项浏览器验收并全部通过：代码块为唯一块时点击上方空白在文档开头新增空段落且光标落进新行（`selectionFrom=1`、焦点在 ProseMirror）、空代码块同样补行、首个块是段落或标题时不补行、代码块后跟段落时点击上方只在开头补行、补行后再次点击不再插入、点击代码块内部不补行、块间空白点击回归通过。本次同时修复顶部补行接线：回调需传 `editor.view`（ProseMirror EditorView），此前误传 TipTap Editor 实例（无 `.dom`/`.dispatch`/`.editable`）导致该入口从未生效。验收页与脚本为临时文件，已删除。
- 2026-09-24：Markdown 格式识别通过 16 项新增单元测试（前端共 139 项）、前端生产构建和后端语法检查（后端无改动）；用临时无头 Chrome + CDP 加载真实 TipTap 编辑器（与 Editor.vue 一致的扩展组合，含 Table/TaskList/CodeBlockWithCopy/Collaboration）完成 14 项浏览器验收并全部通过：混合文档一次识别返回 10 组、H1/H2 标题、引用两行合并、分割线、无序/有序列表、任务列表勾选状态、表格（表头 + 数据行、分隔行带竖线）、代码围栏（js 语言高亮生效）、列表项行内加粗、普通段落未误转、再次识别返回 0、无残留 Markdown 标记文本。验收页与脚本为临时文件，已删除。
