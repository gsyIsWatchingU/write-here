{
  "name": "write-here",
  "version": "1.0.0",
  "description": "轻量级文档编辑器 - 本地前端连接 GPU 后端",
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  }
}
